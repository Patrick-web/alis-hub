// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * PackageService orchestrates the Manage Packages flow: scan → GeneratePackageScripts → run scripts.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * CancelPackageRun cancels a running script.
 * @param {string} runID
 * @returns {$CancellablePromise<void>}
 */
export function CancelPackageRun(runID) {
    return $Call.ByID(1669519566, runID);
}

/**
 * CheckVenvExists returns true if a .venv directory exists at the product build root.
 * @param {string} org
 * @param {string} product
 * @returns {$CancellablePromise<boolean>}
 */
export function CheckVenvExists(org, product) {
    return $Call.ByID(182031158, org, product);
}

/**
 * PollPackageRun returns new output since offset. Reuses LocalBuildChunk from buildservice.
 * @param {string} runID
 * @param {number} offset
 * @returns {$CancellablePromise<$models.LocalBuildChunk | null>}
 */
export function PollPackageRun(runID, offset) {
    return $Call.ByID(1401151733, runID, offset).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * PreparePackageScripts scans the neuron build directory for language manifests, then calls
 * VscodeService/GeneratePackageScripts to obtain the shell commands for each folder.
 * @param {string} org
 * @param {string} product
 * @param {string} neuron
 * @param {string} version
 * @returns {$CancellablePromise<$models.PackageScript[]>}
 */
export function PreparePackageScripts(org, product, neuron, version) {
    return $Call.ByID(972249452, org, product, neuron, version).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * ResizePackageTerminal updates the PTY window size, keeping line-wrapping correct.
 * @param {string} runID
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<void>}
 */
export function ResizePackageTerminal(runID, cols, rows) {
    return $Call.ByID(2164536571, runID, cols, rows);
}

/**
 * StartPackageScript launches a command in a PTY so the terminal is fully interactive.
 * @param {string} runID
 * @param {string} command
 * @param {string} workDir
 * @returns {$CancellablePromise<void>}
 */
export function StartPackageScript(runID, command, workDir) {
    return $Call.ByID(2253516098, runID, command, workDir);
}

/**
 * StartVenvSetup launches the Python venv creation command as a PTY process.
 * Mirrors the extension's Imt() — fires without blocking.
 * @param {string} runID
 * @param {string} org
 * @param {string} product
 * @returns {$CancellablePromise<void>}
 */
export function StartVenvSetup(runID, org, product) {
    return $Call.ByID(1646026243, runID, org, product);
}

/**
 * WritePackageInput sends keystrokes to the PTY stdin of a running process.
 * @param {string} runID
 * @param {string} data
 * @returns {$CancellablePromise<void>}
 */
export function WritePackageInput(runID, data) {
    return $Call.ByID(2728173072, runID, data);
}

// Private type creation functions
const $$createType0 = $models.LocalBuildChunk.createFrom;
const $$createType1 = $Create.Nullable($$createType0);
const $$createType2 = $models.PackageScript.createFrom;
const $$createType3 = $Create.Array($$createType2);
