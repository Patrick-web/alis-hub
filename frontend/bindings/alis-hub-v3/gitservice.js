// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * GitService provides local git operations for the block merge flow.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as application$0 from "../github.com/wailsapp/wails/v3/pkg/application/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * AbortMerge aborts an in-progress merge.
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function AbortMerge(repoPath) {
    return $Call.ByID(6565443, repoPath);
}

/**
 * CheckoutBranch switches to an existing branch.
 * @param {string} repoPath
 * @param {string} branchName
 * @returns {$CancellablePromise<void>}
 */
export function CheckoutBranch(repoPath, branchName) {
    return $Call.ByID(3127373489, repoPath, branchName);
}

/**
 * Commit creates a commit with the given message.
 * @param {string} repoPath
 * @param {string} message
 * @returns {$CancellablePromise<void>}
 */
export function Commit(repoPath, message) {
    return $Call.ByID(471482348, repoPath, message);
}

/**
 * CompleteMerge finalises the merge with a commit (non-interactive).
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function CompleteMerge(repoPath) {
    return $Call.ByID(4202278426, repoPath);
}

/**
 * CreateBranch creates and switches to a new branch.
 * @param {string} repoPath
 * @param {string} branchName
 * @returns {$CancellablePromise<void>}
 */
export function CreateBranch(repoPath, branchName) {
    return $Call.ByID(301706189, repoPath, branchName);
}

/**
 * DiscardFile discards working tree changes for a file.
 * @param {string} repoPath
 * @param {string} filePath
 * @returns {$CancellablePromise<void>}
 */
export function DiscardFile(repoPath, filePath) {
    return $Call.ByID(3427842007, repoPath, filePath);
}

/**
 * GetBranches returns all local and remote branches.
 * @param {string} repoPath
 * @returns {$CancellablePromise<$models.GitBranch[]>}
 */
export function GetBranches(repoPath) {
    return $Call.ByID(3919711711, repoPath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * GetCommitFileDiff returns the diff for a single file within a specific commit.
 * @param {string} repoPath
 * @param {string} hash
 * @param {string} filePath
 * @returns {$CancellablePromise<$models.GitFileDiff | null>}
 */
export function GetCommitFileDiff(repoPath, hash, filePath) {
    return $Call.ByID(2802220399, repoPath, hash, filePath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * GetCommitFiles returns the list of files changed by a specific commit.
 * @param {string} repoPath
 * @param {string} hash
 * @returns {$CancellablePromise<$models.CommitFile[]>}
 */
export function GetCommitFiles(repoPath, hash) {
    return $Call.ByID(434630353, repoPath, hash).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * GetConflictContent reads a conflicted file and parses its conflict hunks.
 * @param {string} repoPath
 * @param {string} filePath
 * @returns {$CancellablePromise<$models.ConflictFileContent | null>}
 */
export function GetConflictContent(repoPath, filePath) {
    return $Call.ByID(2044518434, repoPath, filePath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType7($result);
    }));
}

/**
 * GetConflictFiles returns the list of files with unmerged conflicts.
 * @param {string} repoPath
 * @returns {$CancellablePromise<string[]>}
 */
export function GetConflictFiles(repoPath) {
    return $Call.ByID(652532720, repoPath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType8($result);
    }));
}

/**
 * GetCurrentBranch returns the name of the currently checked-out branch.
 * @param {string} repoPath
 * @returns {$CancellablePromise<string>}
 */
export function GetCurrentBranch(repoPath) {
    return $Call.ByID(2144274600, repoPath);
}

/**
 * GetFileDiff returns old and new file content plus diff hunks for the diff viewer.
 * staged=true shows index vs HEAD; staged=false shows working tree vs index.
 * @param {string} repoPath
 * @param {string} filePath
 * @param {boolean} staged
 * @returns {$CancellablePromise<$models.GitFileDiff | null>}
 */
export function GetFileDiff(repoPath, filePath, staged) {
    return $Call.ByID(1333579366, repoPath, filePath, staged).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * GetLog returns commit history for the git graph.
 * @param {string} repoPath
 * @param {number} limit
 * @returns {$CancellablePromise<$models.GitCommit[]>}
 */
export function GetLog(repoPath, limit) {
    return $Call.ByID(1590913447, repoPath, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType10($result);
    }));
}

/**
 * GetProductRepoPaths returns the local build and define repo paths for the given product.
 * org and product are short names (e.g. "voyage", "vp").
 * @param {string} org
 * @param {string} product
 * @returns {$CancellablePromise<$models.ProductRepoPaths | null>}
 */
export function GetProductRepoPaths(org, product) {
    return $Call.ByID(674422590, org, product).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * GetStatus returns staged, unstaged, and untracked file lists.
 * @param {string} repoPath
 * @returns {$CancellablePromise<$models.GitStatus | null>}
 */
export function GetStatus(repoPath) {
    return $Call.ByID(2522644849, repoPath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType14($result);
    }));
}

/**
 * PullOrigin pulls the current branch from origin, streaming output.
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function PullOrigin(repoPath) {
    return $Call.ByID(1482641752, repoPath);
}

/**
 * PushOrigin pushes the current branch to origin, streaming output.
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function PushOrigin(repoPath) {
    return $Call.ByID(3747123927, repoPath);
}

/**
 * SaveConflictResolution replaces each conflict marker block in the file with the
 * corresponding resolved lines (one entry per hunk, in order), writes the file, and
 * stages it with git add. resolutions[i] is the newline-joined content for hunk i.
 * @param {string} repoPath
 * @param {string} filePath
 * @param {string[]} resolutions
 * @returns {$CancellablePromise<void>}
 */
export function SaveConflictResolution(repoPath, filePath, resolutions) {
    return $Call.ByID(1423658464, repoPath, filePath, resolutions);
}

/**
 * @param {application$0.App | null} app
 * @returns {$CancellablePromise<void>}
 */
export function SetApp(app) {
    return $Call.ByID(1890383270, app);
}

/**
 * StageAll stages all changes.
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function StageAll(repoPath) {
    return $Call.ByID(4059387404, repoPath);
}

/**
 * StageFile stages a single file.
 * @param {string} repoPath
 * @param {string} filePath
 * @returns {$CancellablePromise<void>}
 */
export function StageFile(repoPath, filePath) {
    return $Call.ByID(4185947995, repoPath, filePath);
}

/**
 * StartLocalMerge runs: git fetch, checkout master, pull, then merge origin/{branchName}.
 * Returns conflict file list if exit code 1 (conflicts detected).
 * Git command output is streamed via "git:log" Wails events.
 * @param {string} repoPath
 * @param {string} branchName
 * @returns {$CancellablePromise<$models.LocalMergeResult | null>}
 */
export function StartLocalMerge(repoPath, branchName) {
    return $Call.ByID(2277677406, repoPath, branchName).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType16($result);
    }));
}

/**
 * UnstageFile removes a file from the staging area.
 * @param {string} repoPath
 * @param {string} filePath
 * @returns {$CancellablePromise<void>}
 */
export function UnstageFile(repoPath, filePath) {
    return $Call.ByID(3130933074, repoPath, filePath);
}

/**
 * WatchRepo begins watching repoPath/.git for external changes.
 * Idempotent — safe to call multiple times for the same path.
 * @param {string} repoPath
 * @returns {$CancellablePromise<void>}
 */
export function WatchRepo(repoPath) {
    return $Call.ByID(422539044, repoPath);
}

// Private type creation functions
const $$createType0 = $models.GitBranch.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = $models.GitFileDiff.createFrom;
const $$createType3 = $Create.Nullable($$createType2);
const $$createType4 = $models.CommitFile.createFrom;
const $$createType5 = $Create.Array($$createType4);
const $$createType6 = $models.ConflictFileContent.createFrom;
const $$createType7 = $Create.Nullable($$createType6);
const $$createType8 = $Create.Array($Create.Any);
const $$createType9 = $models.GitCommit.createFrom;
const $$createType10 = $Create.Array($$createType9);
const $$createType11 = $models.ProductRepoPaths.createFrom;
const $$createType12 = $Create.Nullable($$createType11);
const $$createType13 = $models.GitStatus.createFrom;
const $$createType14 = $Create.Nullable($$createType13);
const $$createType15 = $models.LocalMergeResult.createFrom;
const $$createType16 = $Create.Nullable($$createType15);
