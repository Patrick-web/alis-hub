// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * PRService exposes pull request operations to the frontend.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as application$0 from "../github.com/wailsapp/wails/v3/pkg/application/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * AddPRComment posts a conversation comment and returns it.
 */
export function AddPRComment(org: string, product: string, repo: string, $number: number, body: string): $CancellablePromise<$models.PRComment | null> {
    return $Call.ByID(3283026149, org, product, repo, $number, body).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * CreatePR opens a pull request. head and base are branch names.
 */
export function CreatePR(org: string, product: string, repo: string, title: string, body: string, head: string, base: string): $CancellablePromise<$models.ForgejoPR | null> {
    return $Call.ByID(593418113, org, product, repo, title, body, head, base).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * CurrentUser returns the signed-in Forgejo identity.
 */
export function CurrentUser(org: string, product: string, repo: string): $CancellablePromise<$models.PRUser | null> {
    return $Call.ByID(2315127775, org, product, repo).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * GetCommitDiff returns one commit's diff, split per file, for the Commits tab.
 */
export function GetCommitDiff(org: string, product: string, repo: string, sha: string): $CancellablePromise<$models.PRDiff | null> {
    return $Call.ByID(2863588179, org, product, repo, sha).then(($result: any) => {
        return $$createType7($result);
    });
}

/**
 * GetPR fetches a single pull request. The list endpoint's view of mergeability
 * and counts goes stale as soon as anyone pushes, so the detail view refetches
 * rather than trusting what the list said when it was opened.
 */
export function GetPR(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.ForgejoPR | null> {
    return $Call.ByID(3252730047, org, product, repo, $number).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * GetPRComments returns the conversation comments on a pull request.
 */
export function GetPRComments(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.PRCommentList | null> {
    return $Call.ByID(4177420801, org, product, repo, $number).then(($result: any) => {
        return $$createType9($result);
    });
}

/**
 * GetPRCommits returns the commits in a pull request.
 */
export function GetPRCommits(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.PRCommitList | null> {
    return $Call.ByID(493265439, org, product, repo, $number).then(($result: any) => {
        return $$createType11($result);
    });
}

/**
 * GetPRDiff returns the whole pull request diff, split per file.
 * 
 * One request, and no local clone: the previous implementation ran
 * `git diff origin/base...origin/head` per file, which needed the repo cloned
 * with both branches present on origin, silently produced an empty diff for a
 * fork PR, and returned a nil error however badly it failed.
 */
export function GetPRDiff(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.PRDiff | null> {
    return $Call.ByID(2205911476, org, product, repo, $number).then(($result: any) => {
        return $$createType7($result);
    });
}

/**
 * GetPRFiles returns the files changed in a pull request.
 */
export function GetPRFiles(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.PRFileList | null> {
    return $Call.ByID(3710965990, org, product, repo, $number).then(($result: any) => {
        return $$createType13($result);
    });
}

/**
 * GetPRReviews returns the submitted reviews on a pull request.
 */
export function GetPRReviews(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.PRReview[]> {
    return $Call.ByID(104108476, org, product, repo, $number).then(($result: any) => {
        return $$createType15($result);
    });
}

/**
 * GetReviewComments returns the inline comments belonging to one review.
 */
export function GetReviewComments(org: string, product: string, repo: string, $number: number, reviewID: number): $CancellablePromise<$models.PRReviewComment[]> {
    return $Call.ByID(1294956715, org, product, repo, $number, reviewID).then(($result: any) => {
        return $$createType17($result);
    });
}

/**
 * InvalidateProduct drops cached remotes for a product. Called when the
 * workspace switches products.
 */
export function InvalidateProduct(org: string, product: string): $CancellablePromise<void> {
    return $Call.ByID(227470253, org, product);
}

/**
 * ListPRs returns pull requests for a product's repo. state is "open",
 * "closed", or "all"; it defaults to "open".
 */
export function ListPRs(org: string, product: string, repo: string, state: string): $CancellablePromise<$models.PRList | null> {
    return $Call.ByID(3309884414, org, product, repo, state).then(($result: any) => {
        return $$createType19($result);
    });
}

/**
 * MergePR merges a pull request. style is "merge", "rebase", or "squash", and is
 * checked against what the repo permits before the request goes out.
 */
export function MergePR(org: string, product: string, repo: string, $number: number, style: string, deleteBranch: boolean): $CancellablePromise<void> {
    return $Call.ByID(1005472291, org, product, repo, $number, style, deleteBranch);
}

/**
 * PRsAvailable reports whether the repo is an Alis-hosted Forgejo repo with
 * pull requests enabled. Unlike the IsForgejo it replaces, this asks the CLI and
 * the server rather than a local clone, and returns the reason on failure so the
 * UI can show it instead of latching "not available" forever.
 */
export function PRsAvailable(org: string, product: string, repo: string): $CancellablePromise<boolean> {
    return $Call.ByID(1137286877, org, product, repo);
}

/**
 * RepoInfo returns the repository settings the PR UI needs.
 */
export function RepoInfo(org: string, product: string, repo: string): $CancellablePromise<$models.PRRepoInfo | null> {
    return $Call.ByID(3466187039, org, product, repo).then(($result: any) => {
        return $$createType21($result);
    });
}

export function SetApp(app: application$0.App | null): $CancellablePromise<void> {
    return $Call.ByID(456388784, app);
}

/**
 * SetPRReady takes a draft out of draft by stripping the WIP marker from its
 * title. It refuses rather than guessing when there is no marker to strip, so it
 * can never silently rename a PR.
 */
export function SetPRReady(org: string, product: string, repo: string, $number: number): $CancellablePromise<$models.ForgejoPR | null> {
    return $Call.ByID(4288484026, org, product, repo, $number).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * SubmitReview submits a review. event is "APPROVED", "REQUEST_CHANGES", or
 * "COMMENT". comments may be empty for a review that only carries a body.
 */
export function SubmitReview(org: string, product: string, repo: string, $number: number, event: string, body: string, comments: $models.ReviewDraftComment[]): $CancellablePromise<$models.PRReview | null> {
    return $Call.ByID(2805551291, org, product, repo, $number, event, body, comments).then(($result: any) => {
        return $$createType22($result);
    });
}

// Private type creation functions
const $$createType0 = $models.PRComment.createFrom;
const $$createType1 = $Create.Nullable($$createType0);
const $$createType2 = $models.ForgejoPR.createFrom;
const $$createType3 = $Create.Nullable($$createType2);
const $$createType4 = $models.PRUser.createFrom;
const $$createType5 = $Create.Nullable($$createType4);
const $$createType6 = $models.PRDiff.createFrom;
const $$createType7 = $Create.Nullable($$createType6);
const $$createType8 = $models.PRCommentList.createFrom;
const $$createType9 = $Create.Nullable($$createType8);
const $$createType10 = $models.PRCommitList.createFrom;
const $$createType11 = $Create.Nullable($$createType10);
const $$createType12 = $models.PRFileList.createFrom;
const $$createType13 = $Create.Nullable($$createType12);
const $$createType14 = $models.PRReview.createFrom;
const $$createType15 = $Create.Array($$createType14);
const $$createType16 = $models.PRReviewComment.createFrom;
const $$createType17 = $Create.Array($$createType16);
const $$createType18 = $models.PRList.createFrom;
const $$createType19 = $Create.Nullable($$createType18);
const $$createType20 = $models.PRRepoInfo.createFrom;
const $$createType21 = $Create.Nullable($$createType20);
const $$createType22 = $Create.Nullable($$createType14);
